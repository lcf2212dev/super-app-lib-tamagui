export { default as DscButton } from './components/Button/Button';
export { default as tamaguiConfig } from './tamagui.config';

export function multiply(a: number, b: number): number {
  return a * b;
}